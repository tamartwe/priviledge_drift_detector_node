# eslint-config-airbnb-base@15 declares a peer dep on eslint@^7||^8.
# We run ESLint 10 via the flat-config API; the config works correctly at
# runtime but npm's strict peer resolution rejects it. This flag silences
# the conflict so `npm install` works out of the box in CI and on fresh clones.
legacy-peer-deps=true
